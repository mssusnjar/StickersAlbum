namespace Player
{
    internal static class PlayerConstants
    {
        internal const int MaxPacksCount = 3;

        internal const int AddPacksCount = 2;

        internal const int StickersPerPack = 5;

        internal const int TotalStickers = 80;

        internal const int InitialCoins = 60;

        internal const int CoinsForPack = 30;

        internal const int MinutesForNewPacks = 360;
    }
}
